<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>File Upload Success</title>
</head>
<body>
<h1>File Upload Successful for {{ productName }} ({{ release }})!</h1>
<p>Your files have been uploaded successfully.</p>

{% if files is not empty %}
    <h2>Uploaded Files:</h2>
    <ul>
        {% for file in files %}
            <li>{{ file }}</li>
        {% endfor %}
    </ul>
{% else %}
    <p>No files were uploaded.</p>
{% endif %}
</body>
</html>
