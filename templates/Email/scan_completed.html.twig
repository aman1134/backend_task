<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>File Scan is Completed</title>
</head>
<body>
<h1>File scan is Completed for {{ productName }} ({{ release }})!</h1>
{% if files is not empty %}
    <h2>Scanned Files are having {{ count }} vulnerabilities :</h2>
    <ul>
        {% for file in files %}
            <li>{{ file }}</li>
        {% endfor %}
    </ul>
{% else %}
    <p>No files are scanned.</p>
{% endif %}
</body>
</html>
